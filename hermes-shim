#!/bin/sh
# Drop to the hermes user before running the venv'd hermes CLI.
# Upstream installs s6-overlay (which ships s6-setuidgid); gosu is no
# longer available in the image.
exec s6-setuidgid hermes /opt/hermes/.venv/bin/hermes "$@"
